# Test39 — nested structural leaf completion

Reader page28 was supplied directly after page27 phone acceptance. Test38 publishes two coarse unproved full-width slabs on this six-panel page, so every tap repeats one of two invalid giant crops.

Test39 adds a bounded completion for exactly that evidence class: the only surviving map must still be the original pair of unproved stacked legacy slabs, and a tap-independent structural grid must prove a complete **2 top + 1 middle strip + 3 bottom** leaf hierarchy. The grid also retracts a horizontal split inside a narrow vertical panel when one child is a strict scene, the other is substantial dark textured artwork, and the unsplit parent is itself a strict scene. This prevents artwork inside the bottom-middle panel from shaving off its lower portion.

Private-fixture result on Reader page28: **2 → 6** page-wide identities. Structural analysis is 585×900, coverage **0.973**, with five retained structural splits after one false narrow-panel split is retracted. All six emitted owners are orthogonal structural-grid frames.

Actual Reader/NthPageDeck verification: **30/30** center/left/right/north/south taps across all six panels selected the intended owner, with zero page errors. Direct descriptor comparisons show Reader page23 and page27 remain byte-for-byte identical to Test38. A targeted sweep of every other preserved two-entry page (4, 8, 47, 49, 51, 53, 59, 62, 63 and73) is also byte-for-byte unchanged; only page28 changes.

Runtime contains no comic title, filename, reader page number, image hash, tap coordinate or stored crop. `nested-grid-contract.cjs` synthesizes the 2+1+3 hierarchy with a false dark internal rail inside the narrow bottom-middle scene and requires the full panel to be reunited. Android build and page28 phone acceptance are separate gates.


## Phone acceptance update — page28

The user advanced from page28 to page32 after testing Test39. Reader page28 is accepted for the manual page-by-page queue.
